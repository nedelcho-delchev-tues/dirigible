## Native Harmonia chart directive for this report's layout type (a generation-time constant).
#if($layoutType == "REPORT_LINE")#set($chartDirective = "x-h-chart-line")#elseif($layoutType == "REPORT_PIE")#set($chartDirective = "x-h-chart-pie")#elseif($layoutType == "REPORT_DOUGHNUT")#set($chartDirective = "x-h-chart-doughnut")#elseif($layoutType == "REPORT_POLARAREA")#set($chartDirective = "x-h-chart-polar-area")#elseif($layoutType == "REPORT_RADAR")#set($chartDirective = "x-h-chart-radar")#else#set($chartDirective = "x-h-chart-bar")#end
<!-- Generated by Eclipse Dirigible. Report chart view for ${name} (native Harmonia chart). -->
<div x-data="${name}ReportPage" class="vbox size-full">
  <div x-h-toolbar data-variant="transparent">
    <span x-h-toolbar-title class="text-2xl font-semibold tracking-tight">${name}</span>
    <div x-h-toolbar-spacer></div>
    <button x-h-button data-variant="outline" data-size="sm" @click="load()"><svg role="presentation" x-h-lucide data-lucide="refresh-cw"></svg><span x-text="T('$projectName:${tprefix}.defaults.refresh', 'Refresh')"></span></button>
    <button x-h-button data-variant="outline" data-size="sm" @click="printChart()"><svg role="presentation" x-h-lucide data-lucide="printer"></svg><span x-text="T('$projectName:${tprefix}.defaults.print', 'Print')"></span></button>
  </div>
  <div x-show="state === 'loading'" class="p-4"><div x-h-spinner></div></div>
  <div x-show="state === 'error'" x-h-alert data-variant="negative" role="alert" class="m-4"><div x-h-alert-description x-text="error"></div></div>
  <div x-show="state === 'empty'" x-h-text.muted class="p-4">The ${name} report returned no rows.</div>
  <div x-show="state === 'default'" class="p-4" style="height: 70vh;">
    <div x-ref="chartEl" ${chartDirective}="chartConfig" class="size-full"></div>
  </div>
</div>
