<!--
  Generated by Eclipse Dirigible based on model and template.
  Master-detail view for ${name}: master list (left) + the selected record's detail panels
  (right), via x-h-split. Root x-data binds the generated ${name}MasterPage; detail panels are
  generic detailPanel components driven by the runtime registry (App.detailsFor).
-->
## Role-scoped field (intent `visibleTo:`): rendered only for a caller the server actually serves it
## to - the page asked the controller which fields it withholds (canSee). Emits nothing for an
## unrestricted property. The redaction applied to every response stays the authoritative one; this
## only keeps a control that could never show or save a value off the page.
#macro(visibleGate $property)#if($property.roleRead) x-show="canSee('${property.name}')"#end#end
<div x-data="${name}MasterPage" class="vbox size-full">

  <div x-h-toolbar data-variant="transparent">
    <span x-h-toolbar-title class="shrink-0 text-2xl font-semibold tracking-tight" x-text="T('$projectName:${tprefix}.t.${dataName}_plural', '${menuLabel}')"></span>
    <div x-h-toolbar-spacer></div>
    <div x-h-input-group data-size="sm">
      <input x-h-input.group type="text" :placeholder="T('$projectName:${tprefix}.messages.inputSearch', 'Search ${name}...', { name: T('$projectName:${tprefix}.t.${dataName}', '${name}') })" x-model="searchTerm" @input="page = 1; refreshIcons()" />
      <div x-h-input-group-addon data-align="inline-start"><svg x-h-icon data-icon="search" class="size-4" role="img" aria-label="search"></svg></div>
    </div>
#if($calendarView)
    <!-- The entity also renders on a calendar (intent `view: calendar|range`), which owns /${name};
         this master page lives at /${name}/list. -->
    <button x-h-button data-variant="outline" data-size="sm" @click="goCalendar()"><i role="img" x-h-lucide data-lucide="calendar"></i><span x-text="T('$projectName:${tprefix}.defaults.calendar', 'Calendar')"></span></button>
#end
#if($slotsView)
    <!-- The entity also renders as a slot picker (intent `view: slots`), which owns /${name};
         this master page lives at /${name}/list. -->
    <button x-h-button data-variant="outline" data-size="sm" @click="goSlots()"><i role="img" x-h-lucide data-lucide="clock"></i><span x-text="T('$projectName:${tprefix}.defaults.slots', 'Slots')"></span></button>
#end
    <button x-h-button data-variant="primary" @click="newMaster()"><i role="img" x-h-lucide data-lucide="plus"></i><span x-text="T('$projectName:${tprefix}.defaults.new', 'New')"></span></button>
    <!-- Export the filtered rows as CSV / print them (Save as PDF via the browser dialog). -->
    <button x-h-button data-variant="outline" data-size="sm" :disabled="filteredMasters.length === 0" @click="exportCsv()">
      <i role="img" x-h-lucide data-lucide="download"></i><span x-text="T('$projectName:${tprefix}.defaults.export', 'Export')"></span>
    </button>
    <button x-h-button data-variant="outline" data-size="sm" :disabled="filteredMasters.length === 0" @click="printList()">
      <i role="img" x-h-lucide data-lucide="printer"></i><span x-text="T('$projectName:${tprefix}.defaults.print', 'Print')"></span>
    </button>
    <!-- Developer-contributed page actions for this view (customActions extension point). -->
    <template x-for="action in $store.customActions.getActions(caView, 'page')" :key="action.id">
      <button x-h-button data-variant="outline" data-size="sm" @click="$store.customActions.trigger(action)">
        <span x-text="action.translation &amp;&amp; action.translation.key ? T(action.translation.key, action.label, action.translation.options || {}) : action.label"></span>
      </button>
    </template>
  </div>

  <div x-show="state === 'loading'" class="p-4"><div x-h-spinner></div></div>
  <div x-show="state === 'error'" x-h-alert data-variant="negative" role="alert" class="m-4">
    <div x-h-alert-description x-text="error"></div>
  </div>

  <div x-show="state === 'default' || state === 'empty'" x-h-split class="bk-stretch flex-1" data-orientation="horizontal" data-variant="border">

    <!-- Master list (fills the whole width until a record is selected) -->
    <div x-h-split-panel data-size="60%" data-min="280">
      <div x-h-table-container.scroll class="size-full">
        <table x-h-table data-borders="rows">
          <thead x-h-table-header>
            <tr x-h-table-row>
#foreach($property in $properties)
#if(!$property.dataAutoIncrement && $property.widgetIsMajor)
              <th x-h-table-head scope="col"#if($property.isNumberType) class="text-right"#end#visibleGate($property)>${property.name}</th>
#end
#end
              <th x-h-table-head scope="col"></th>
            </tr>
          </thead>
          <tbody x-h-table-body>
            <tr x-show="filteredMasters.length === 0" x-h-table-row><td x-h-table-cell x-text="T('$projectName:${tprefix}.messages.noData', 'No ${name}.')"></td></tr>
            <template x-for="row in pagedMasters" :key="row.${primaryKeysString}">
              <tr x-h-table-row data-hoverable="true" :data-state="isSelected(row) ? 'selected' : ''" @click="selectRow(row)">
#foreach($property in $properties)
#if(!$property.dataAutoIncrement && $property.widgetIsMajor)
#if($property.widgetType == "DOCUMENT_STATUS")
                <!-- EntityStatus column: the resolved status name as a coloured badge pill. -->
                <td x-h-table-cell#visibleGate($property)><span x-h-badge :data-variant="statusVariant(lookupText('${property.name}', row.${property.name}))" x-show="lookupText('${property.name}', row.${property.name})" x-text="lookupText('${property.name}', row.${property.name})"></span></td>
#elseif($property.widgetType == "DROPDOWN")
                <td x-h-table-cell#visibleGate($property) x-text="lookupText('${property.name}', row.${property.name})"></td>
#elseif($property.widgetType == "MULTISELECT")
                <td x-h-table-cell#visibleGate($property) x-text="multiLookupText('${property.name}', row.${property.name})"></td>
#elseif($property.isDateType)
                <td x-h-table-cell#visibleGate($property) x-text="displayValue(row.${property.name}, true)"></td>
#elseif($property.isFloatType)
                <td x-h-table-cell class="text-right"#visibleGate($property) x-text="formatNumber(row.${property.name}, '${property.formatPattern}')"></td>
#elseif($property.isNumberType)
                <td x-h-table-cell class="text-right"#visibleGate($property) x-text="displayValue(row.${property.name})"></td>
#else
                <td x-h-table-cell#visibleGate($property) x-text="displayValue(row.${property.name})"></td>
#end
#end
#end
                <td x-h-table-cell data-row-actions @click.stop>
                  <button x-h-menu-trigger.dropdown x-h-table-cell-button aria-label="Row actions"><i role="img" x-h-lucide data-lucide="ellipsis"></i></button>
                  <ul x-h-menu data-align="bottom-end">
                    <li x-h-menu-item @click="previewMaster(row)" x-text="T('$projectName:${tprefix}.defaults.preview', 'Preview')"></li>
#if($immutableStatusProperty || $immutableAlways)
                    <!-- Edit/Delete are hidden for an immutable record (the controller would 409);
                         Preview stays - the record remains readable. -->
                    <li x-h-menu-item x-show="!isRowImmutable(row)" @click="editMaster(row)" x-text="T('$projectName:${tprefix}.defaults.edit', 'Edit')"></li>
                    <li x-h-menu-separator x-show="!isRowImmutable(row)"></li>
                    <li x-h-menu-item x-show="!isRowImmutable(row)" data-variant="negative" @click="askDelete(row)" x-text="T('$projectName:${tprefix}.defaults.delete', 'Delete')"></li>
#else
                    <li x-h-menu-item @click="editMaster(row)" x-text="T('$projectName:${tprefix}.defaults.edit', 'Edit')"></li>
                    <li x-h-menu-separator></li>
                    <li x-h-menu-item data-variant="negative" @click="askDelete(row)" x-text="T('$projectName:${tprefix}.defaults.delete', 'Delete')"></li>
#end
                  </ul>
                </td>
              </tr>
            </template>
          </tbody>
        </table>
      </div>
      <div class="hbox items-center gap-2 p-2" x-show="(state === 'default' || state === 'empty') && pageCount > 1">
        <button x-h-button data-variant="outline" data-size="sm" :disabled="page <= 1" @click="goPage(page - 1)"><i role="img" x-h-lucide data-lucide="chevron-left"></i><span x-text="T('$projectName:${tprefix}.defaults.previous', 'Prev')"></span></button>
        <span x-h-text.muted x-text="T('$projectName:${tprefix}.defaults.pageOf', 'Page {{page}} / {{total}}', { page: page, total: pageCount })"></span>
        <button x-h-button data-variant="outline" data-size="sm" :disabled="page >= pageCount" @click="goPage(page + 1)"><span x-text="T('$projectName:${tprefix}.defaults.next', 'Next')"></span><i role="img" x-h-lucide data-lucide="chevron-right"></i></button>
      </div>
    </div>

    <!-- Detail area — hidden (0%) until a master record is selected, then ~40%. -->
    <div x-h-split-panel data-size="40%" :data-hidden="!selectedId">
      <div x-show="!selectedId" x-h-info-page class="p-4">
        <div x-h-info-page-header>
          <div x-h-info-page-media.icon><svg x-h-icon data-icon="circle-info" role="img" aria-label="info"></svg></div>
          <div x-h-info-page-title x-text="T('$projectName:${tprefix}.defaults.selectTitle', 'Select a ${name}', { name: T('$projectName:${tprefix}.t.${dataName}', '${name}') })"></div>
          <div x-h-info-page-description>Choose a record on the left to see and manage its details.</div>
        </div>
      </div>

      <div x-show="selectedId" class="vbox gap-4 p-4 overflow-auto">
        <div x-h-toolbar data-variant="transparent" data-size="sm">
          <span x-h-toolbar-title class="shrink-0" x-text="T('$projectName:${tprefix}.t.${dataName}', '${entityLabel}') + ' #' + selectedId"></span>
          <div x-h-toolbar-spacer></div>
#if($scopedCalendars)
          <!-- Scoped calendars (intent `calendar.scope`): an entity whose calendar filters its events
               to the selected record. The filter is a query parameter on the calendar's own route,
               so without a button here the URL is knowledge the user has to type. -->
#foreach($scopedCalendar in $scopedCalendars)
          <button x-h-button data-variant="outline" data-size="sm" @click="openScopedCalendar('${scopedCalendar.entity}', '${scopedCalendar.scopeProperty}', selectedId)"><i role="img" x-h-lucide data-lucide="calendar-days"></i><span x-text="T('$projectName:${tprefix}.t.${scopedCalendar.dataName}_plural', '${scopedCalendar.label}')"></span></button>
#end
#end
          <button x-h-button data-variant="outline" data-size="sm" @click="previewMaster(selected)"><i role="img" x-h-lucide data-lucide="eye"></i><span x-text="T('$projectName:${tprefix}.defaults.preview', 'Preview')"></span></button>
#if($immutableStatusProperty || $immutableAlways)
          <!-- Edit/Delete are disabled for an immutable record (the controller would 409). -->
          <button x-h-button data-variant="outline" data-size="sm" :disabled="isRowImmutable(selected)" :title="isRowImmutable(selected) ? T('$projectName:${tprefix}.messages.immutable', 'This record is read-only in its current status - corrections go through the workflow.') : null" @click="editMaster(selected)"><i role="img" x-h-lucide data-lucide="pencil"></i><span x-text="T('$projectName:${tprefix}.defaults.edit', 'Edit')"></span></button>
          <button x-h-button data-variant="transparent" data-size="sm" :disabled="isRowImmutable(selected)" @click="askDelete(selected)" aria-label="Delete"><i role="img" x-h-lucide data-lucide="trash-2"></i></button>
#else
          <button x-h-button data-variant="outline" data-size="sm" @click="editMaster(selected)"><i role="img" x-h-lucide data-lucide="pencil"></i><span x-text="T('$projectName:${tprefix}.defaults.edit', 'Edit')"></span></button>
          <button x-h-button data-variant="transparent" data-size="sm" @click="askDelete(selected)" aria-label="Delete"><i role="img" x-h-lucide data-lucide="trash-2"></i></button>
#end
        </div>

#if($hasProcess)
        <!-- Actionable user tasks for the selected ${name}, one button each (claimed on open). -->
        <div x-show="$store.processTasks.getTasks(selected).length" class="hbox gap-2 flex-wrap">
          <template x-for="task in $store.processTasks.getTasks(selected)" :key="task.id">
            <button x-h-button data-variant="primary" data-size="sm" @click="$store.processTasks.openTask(task)">
              <i role="img" x-h-lucide data-lucide="inbox"></i><span x-text="$store.processTasks.taskName(task)"></span>
            </button>
          </template>
        </div>
#end

        <!-- Developer-contributed per-record actions for this entity (customActions extension point).
             The selected row goes to getActions, so an action its current status can never accept - a
             transition outside its `from:`, a create-from outside its `fromStatus:` - is not offered at
             all (#7073, #7068). -->
        <div x-show="$store.customActions.getActions(caView, 'entity', selected).length" class="hbox gap-2 flex-wrap">
          <template x-for="action in $store.customActions.getActions(caView, 'entity', selected)" :key="action.id">
            <button x-h-button data-variant="outline" data-size="sm" @click="$store.customActions.trigger(action, selectedId)">
              <span x-text="action.translation &amp;&amp; action.translation.key ? T(action.translation.key, action.label, action.translation.options || {}) : action.label"></span>
            </button>
          </template>
        </div>

        <!-- The selected ${name}'s own fields (the table shows only the major ones). Labels are
             translated like everywhere else; read-only / system fields (audit, uuid, ProcessId,
             read-only) are shown only when populated - matching the edit form's read-only sidebar -
             so empty audit columns don't render as blank labelled rows. -->
        <div x-h-card>
          <div x-h-card-header><h3 x-h-card-title x-text="T('$projectName:${tprefix}.defaults.details', 'Details')"></h3></div>
          <div x-h-card-content>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
#foreach($property in $properties)
## ProcessIds is the per-process at-most-once bookkeeping (Process=instanceId pairs) - platform
## plumbing with nothing to say to a reader, unlike ProcessId, which names the flow the record is in.
#if(!$property.dataAutoIncrement && !$property.isHiddenProperty)
#if($property.widgetLabel)#set($plabel = $property.widgetLabelJs)#else#set($plabel = $property.name)#end
#if($property.isDateType)#set($isDate = "true")#else#set($isDate = "false")#end
#if($property.isReadOnlyProperty == "true" || ($property.auditType && $property.auditType != "NONE"))
              <div class="vbox gap-0" x-show="selected?.${property.name} !== null && selected?.${property.name} !== undefined && selected?.${property.name} !== ''">
#else
              <div class="vbox gap-0"#visibleGate($property)>
#end
                <span x-h-text.muted class="text-xs" x-text="T('$projectName:${tprefix}.t.${property.dataName}', '${plabel}')"></span>
#if($property.widgetType == "DOCUMENT_STATUS")
                <span><span x-h-badge :data-variant="statusVariant(lookupText('${property.name}', selected?.${property.name}))" x-show="lookupText('${property.name}', selected?.${property.name})" x-text="lookupText('${property.name}', selected?.${property.name})"></span></span>
#elseif($property.widgetType == "DROPDOWN")
                <span x-text="lookupText('${property.name}', selected?.${property.name})"></span>
#elseif($property.widgetType == "MULTISELECT")
                <span x-text="multiLookupText('${property.name}', selected?.${property.name})"></span>
#elseif($property.isFloatType)
                <span x-text="formatNumber(selected?.${property.name}, '${property.formatPattern}')"></span>
#else
                <span x-text="displayValue(selected?.${property.name}, $isDate)"></span>
#end
              </div>
#end
#end
            </div>
          </div>
        </div>

        <div x-show="detailDefs.length === 0" x-h-text.muted>This ${name} has no detail collections.</div>

        <!-- One detail panel per registered detail; re-keyed on selection so it reloads. -->
        <template x-for="d in detailDefs" :key="d.entity + ':' + selectedId">
          <div x-data="detailPanel(d, selectedId)" x-h-card>
            <div x-h-card-header>
              <h3 x-h-card-title x-text="d.label"></h3>
            </div>
            <div x-h-card-content>
              <div x-show="state === 'loading'"><div x-h-spinner></div></div>
              <div x-show="state === 'error'" x-h-text.muted x-text="error"></div>
              <div x-show="state === 'empty'" x-h-text.muted x-text="T('$projectName:${tprefix}.messages.noData', 'No records yet.')"></div>
              <!-- A calendar detail (intent view: calendar on the composition child) renders the same
                   master-filtered rows as events on an embedded calendar: event-click edits the child,
                   empty-day click creates one with the master FK + the clicked date preset. -->
              <template x-if="def.calendar">
                <div x-show="state === 'default'" style="min-height: 420px">
                  <div x-h-calendar="calCfg" style="height: 420px" @event-click="onEventClick" @date-click="onDateClick"></div>
                </div>
              </template>
              <!-- A files detail (function: Attachment / Snapshot) on the read-only browse pane: the
                   files are downloadable here; upload/remove happen on the record's Edit form. -->
              <template x-if="def.files">
                <div x-show="state === 'default'" class="vbox gap-3">
                  <template x-if="rows.length === 0">
                    <div x-h-info-page>
                      <div x-h-info-page-header>
                        <div x-h-info-page-media.icon><i role="img" x-h-lucide data-lucide="paperclip"></i></div>
                        <div x-h-info-page-title x-text="filesReadOnly ? T('$projectName:${tprefix}.messages.noCopies', 'No copies yet') : T('$projectName:${tprefix}.messages.noFiles', 'No attachments')"></div>
                        <div x-h-info-page-description x-text="filesReadOnly ? T('$projectName:${tprefix}.messages.noCopiesHint', 'A copy is stored automatically when the document is issued.') : T('$projectName:${tprefix}.messages.noFilesHint', 'Files are managed from the record’s Edit form.')"></div>
                      </div>
                    </div>
                  </template>
                  <template x-if="rows.length > 0">
                    <div class="vbox gap-2">
                      <template x-for="row in rows" :key="row[def.primaryKey]">
                        <div x-h-card class="hbox items-center gap-3 p-3">
                          <i role="img" x-h-lucide data-lucide="file-text" class="shrink-0"></i>
                          <div class="vbox gap-0 grow min-w-0">
                            <span class="text-sm truncate" x-text="row.FileName"></span>
                            <span class="text-xs text-muted-foreground" x-text="fileSizeText(row)"></span>
                          </div>
                          <template x-if="filesReadOnly">
                            <a x-h-button data-variant="transparent" data-size="sm" :href="openHref(row)" target="_blank" rel="noopener" aria-label="Print"><i role="img" x-h-lucide data-lucide="printer"></i></a>
                          </template>
                          <a x-h-button data-variant="transparent" data-size="sm" :href="downloadHref(row)" target="_blank" rel="noopener" aria-label="Download"><i role="img" x-h-lucide data-lucide="download"></i></a>
                        </div>
                      </template>
                    </div>
                  </template>
                </div>
              </template>
              <template x-if="!def.calendar &amp;&amp; !def.files">
              <div x-show="state === 'default'" x-h-table-container.scroll style="max-height: 320px">
                <table x-h-table data-borders="rows">
                  <thead x-h-table-header>
                    <tr x-h-table-row>
                      <template x-for="col in def.columns" :key="col.name">
                        <th x-h-table-head scope="col" :class="col.number ? 'text-right' : ''" x-text="T(col.tkey, col.label || col.name)"></th>
                      </template>
                      <th x-h-table-head scope="col"></th>
                    </tr>
                  </thead>
                  <tbody x-h-table-body>
                    <template x-for="row in rows" :key="row[def.primaryKey]">
                      <tr x-h-table-row data-hoverable="true">
                        <template x-for="col in def.columns" :key="col.name">
                          <td x-h-table-cell :class="col.number ? 'text-right' : ''">
                            <template x-if="col.number"><span x-text="cellValue(col, row)"></span></template>
                            <template x-if="!col.number">
                              <div style="white-space: normal; overflow-wrap: break-word; max-width: 28rem;" x-text="cellValue(col, row)"></div>
                            </template>
                          </td>
                        </template>
                        <!-- Read-only browse pane: children are added/edited/deleted from the record's
                             Edit form (where these same panels are editable), NOT here. Only the
                             actionable user tasks (e.g. a triggered review) and a read-only Preview
                             remain. -->
                        <td x-h-table-cell data-row-actions>
                          <template x-for="task in $store.processTasks.getTasks(row)" :key="task.id">
                            <button x-h-button data-variant="primary" data-size="sm" @click="$store.processTasks.openTask(task)">
                              <i role="img" x-h-lucide data-lucide="inbox"></i><span x-text="$store.processTasks.taskName(task)"></span>
                            </button>
                          </template>
                          <button x-h-button data-variant="transparent" data-size="sm" aria-label="Preview" @click="previewRow(row)"><i role="img" x-h-lucide data-lucide="eye"></i></button>
                        </td>
                      </tr>
                    </template>
                  </tbody>
                </table>
              </div>
              </template>
            </div>
          </div>
        </template>

        <!-- ===== Related registers: the records that REFERENCE this one, read-only. Those records are
             owned by their own entities - own lifecycle, own pages, own processes - so this is a
             window onto them and never an editor: a row opens the source's own record page in the
             shared dialog (the source may live in another application entirely). ===== -->
        <template x-for="r in relatedDefs" :key="r.entity + ':' + selectedId">
          <div x-data="relatedPanel(r, selectedId)" x-h-card class="w-full">
            <div x-h-card-header>
              <h3 x-h-card-title x-text="title"></h3>
            </div>
            <div x-h-card-content>
              <div x-show="state === 'loading'"><div x-h-spinner></div></div>
              <div x-show="state === 'error'" x-h-text.muted x-text="error"></div>
              <div x-show="state === 'empty'" x-h-text.muted x-text="T('$projectName:${tprefix}.messages.noData', 'No records yet.')"></div>
              <div x-show="state === 'default'" x-h-table-container.scroll style="max-height: 320px">
                <table x-h-table data-borders="rows">
                  <thead x-h-table-header>
                    <tr x-h-table-row>
                      <template x-for="col in def.columns" :key="col.name">
                        <th x-h-table-head scope="col" :class="col.number ? 'text-right' : ''" x-text="columnHeader(col)"></th>
                      </template>
                      <th x-h-table-head scope="col"></th>
                    </tr>
                  </thead>
                  <tbody x-h-table-body>
                    <template x-for="row in rows" :key="row[def.primaryKey]">
                      <tr x-h-table-row data-hoverable="true">
                        <template x-for="col in def.columns" :key="col.name">
                          <td x-h-table-cell :class="col.number ? 'text-right' : ''">
                            <template x-if="col.number"><span x-text="cellValue(col, row)"></span></template>
                            <template x-if="!col.number">
                              <div style="white-space: normal; overflow-wrap: break-word; max-width: 28rem;" x-text="cellValue(col, row)"></div>
                            </template>
                          </td>
                        </template>
                        <td x-h-table-cell data-row-actions>
                          <button type="button" x-h-button data-variant="transparent" data-size="sm" @click="openRow(row)" :aria-label="T('$projectName:${tprefix}.defaults.open', 'Open')"><i role="img" x-h-lucide data-lucide="external-link"></i></button>
                        </td>
                      </tr>
                    </template>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </template>
      </div>
    </div>
  </div>

  <!-- Master delete confirmation -->
  <div x-h-dialog-overlay :data-open="deleteOpen">
    <div x-h-dialog>
      <div x-h-dialog-header>
        <h2 x-h-dialog-title x-text="T('$projectName:${tprefix}.defaults.deleteTitle', 'Delete ${name}?', { name: T('$projectName:${tprefix}.t.${dataName}', '${name}') })"></h2>
        <button x-h-dialog-close @click="deleteOpen = false" aria-label="Close"><i role="img" x-h-lucide data-lucide="x"></i></button>
      </div>
      <div x-h-dialog-content><p x-text="T('$projectName:${tprefix}.messages.deleteConfirm', 'Are you sure you want to delete this ${name}? This cannot be undone.', { name: T('$projectName:${tprefix}.t.${dataName}', '${name}') })"></p></div>
      <div x-h-dialog-footer>
        <button x-h-button data-variant="outline" @click="deleteOpen = false" :disabled="deleteBusy" x-text="T('$projectName:${tprefix}.defaults.cancel', 'Cancel')"></button>
        <button x-h-button data-variant="negative" @click="confirmDelete()" :disabled="deleteBusy"><span x-show="deleteBusy" x-h-spinner></span><span x-text="T('$projectName:${tprefix}.defaults.delete', 'Delete')"></span></button>
      </div>
    </div>
  </div>

</div>
