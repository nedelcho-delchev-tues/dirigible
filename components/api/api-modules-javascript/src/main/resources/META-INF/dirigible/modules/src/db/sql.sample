import { sql } from "@aerokit/sdk/db";
import { response } from "@aerokit/sdk/http";

let script = sql.getDialect().select().column("FIRST_NAME").column("LAST_NAME").from("CUSTOMERS").build();

response.println(script);

response.flush();
response.close();
