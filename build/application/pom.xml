<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.eclipse.dirigible</groupId>
        <artifactId>dirigible-build-parent</artifactId>
        <version>15.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <name>Build - Application</name>
    <artifactId>dirigible-application</artifactId>
    <packaging>jar</packaging>


    <properties>
        <license.header.location>../../licensing-header.txt</license.header.location>
        <parent.pom.folder>../../</parent.pom.folder>
    </properties>

    <dependencies>
        <!-- Platform -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-core</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Commons -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-commons-config</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-commons-resources</artifactId>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.logging.log4j</groupId>
                    <artifactId>log4j-to-slf4j</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Date Type Utils -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-joda</artifactId>
        </dependency>

        <!-- Data Access -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>

        <!-- WebJars -->
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>webjars-locator</artifactId>
            <version>${webjars.locator.version}</version>
        </dependency>

        <!-- Core -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-core</artifactId>
            <type>pom</type>
        </dependency>

        <!-- Security -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-security-basic</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-security-keycloak</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-security-snowflake</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-security-cognito</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-security-client-registration</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-security-oauth2</artifactId>
        </dependency>

        <!-- Data -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-database</artifactId>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-data-processes</artifactId>
        </dependency>

        <!-- Engine -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-engines</artifactId>
            <type>pom</type>
        </dependency>

        <!-- IDE -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-ide</artifactId>
            <type>pom</type>
        </dependency>

        <!-- UI -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-ui</artifactId>
            <type>pom</type>
        </dependency>

        <!-- API -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-api</artifactId>
            <type>pom</type>
        </dependency>

        <!-- Resources -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-resources-ui</artifactId>
            <type>pom</type>
        </dependency>

        <!-- Templates -->
        <dependency>
            <groupId>org.eclipse.dirigible</groupId>
            <artifactId>dirigible-components-group-templates</artifactId>
            <type>pom</type>
        </dependency>

        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
        </dependency>

        <dependency>
            <groupId>de.codecentric</groupId>
            <artifactId>spring-boot-admin-starter-server</artifactId>
            <version>${spring.admin.version}</version>
        </dependency>
        <dependency>
            <groupId>de.codecentric</groupId>
            <artifactId>spring-boot-admin-starter-client</artifactId>
            <version>${spring.admin.version}</version>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.eclipse.dirigible</groupId>
                <artifactId>dirigible-dependencies</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>org.eclipse.dirigible.DirigibleApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                        <configuration>
                            <classifier>executable</classifier>
                            <!-- ZIP layout launches through PropertiesLauncher, so a plain
                                 `java -jar` honors BOTH -Dloader.path (the /modules drop-in and the
                                 resolved-modules seed) AND the Launcher-Agent-Class manifest entry
                                 (which the JVM only processes for -jar launches). Without
                                 loader.path the launcher behaves exactly like JarLauncher. -->
                            <layout>ZIP</layout>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <!-- the input of the provided-BOM: this module's own resolved runtime
                             dependencies, which are exactly what spring-boot:repackage packs into
                             BOOT-INF/lib (never derived by unzipping the assembled artifact - a
                             reactor-built module's jar does not live at a local-repository path) -->
                        <id>list-provided-dependencies</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>list</goal>
                        </goals>
                        <configuration>
                            <includeScope>runtime</includeScope>
                            <sort>true</sort>
                            <outputFile>${project.build.directory}/provided-dependencies.txt</outputFile>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- stages the BOM generator on its own: the generator is deliberately
                             dependency-free, so the forked java below needs only this one jar on
                             its command line - the full runtime classpath would exceed the Windows
                             CreateProcess length limit (error=206) -->
                        <id>copy-bom-generator</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.eclipse.dirigible</groupId>
                                    <artifactId>dirigible-components-core-dependencies</artifactId>
                                    <version>${project.version}</version>
                                    <outputDirectory>${project.build.directory}/bom-generator</outputDirectory>
                                    <destFileName>bom-generator.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unpack-launcher-agent</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.eclipse.dirigible</groupId>
                                    <artifactId>dirigible-components-core-launcher-agent</artifactId>
                                    <version>${project.version}</version>
                                    <outputDirectory>${project.build.directory}/launcher-agent-root</outputDirectory>
                                    <includes>org/**</includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- the root config points failsafe at the MAIN output (the tests-integrations
                         convention); this module's ITs are ordinary test sources - they verify the
                         packaged executable jar and must never ship inside it -->
                    <testClassesDirectory>${project.build.testOutputDirectory}</testClassesDirectory>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <!-- Pinned: an unpinned antrun resolves to 1.3 under older local Maven installs,
                     where the injection below silently degrades - the known local-vs-CI split behind
                     LauncherAgentDeliveryIT failing on developer machines while green on CI. -->
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <!-- Turns the resolved dependency list into the provided-BOM (a standard
                             dependencyManagement POM) inside the classes, so it ships embedded in
                             the artifact as META-INF/dirigible-provided-bom.xml - the resolver reads
                             it to treat platform-shipped coordinates as provided and to report
                             shadowing. Runs in prepare-package, before the jar is assembled.
                             Forked with ONLY the staged generator jar on the classpath: the full
                             runtime classpath would exceed the Windows CreateProcess command-line
                             length limit (error=206), and a non-forked java is no alternative -
                             Ant installs a SecurityManager for it, which JDK 18+ refuses. -->
                        <id>generate-provided-bom</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <java classname="org.eclipse.dirigible.components.dependencies.ProvidedBomGenerator" classpath="${project.build.directory}/bom-generator/bom-generator.jar" fork="true" failonerror="true">
                                    <arg value="${project.build.directory}/provided-dependencies.txt"/>
                                    <arg value="${project.build.outputDirectory}/META-INF/dirigible-provided-bom.xml"/>
                                    <arg value="${project.version}"/>
                                </java>
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <!-- The launcher agent's classes must sit at the executable jar's ROOT: the
                             JVM loads the Launcher-Agent-Class through the system classloader before
                             main, and only the jar root is on that classpath (BOOT-INF is not).
                             Bound to pre-integration-test because Maven's parent-first plugin merge
                             orders this plugin BEFORE spring-boot:repackage within the package phase;
                             this phase is after package and before the failsafe ITs that verify the
                             delivery. keepcompression preserves the STORED (uncompressed) nested
                             BOOT-INF jars Spring Boot's loader requires. -->
                        <id>inject-launcher-agent</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <!-- The manifest attribute is added HERE, together with the classes it
                                     names, never at package time: a manifest naming an agent class the
                                     archive does not carry aborts `java -jar` before main (JEP 261), so
                                     a build that stops at package must produce a jar with NO agent (it
                                     boots, without restartless dependency delivery) rather than a jar
                                     that cannot boot at all. The jar task merges the attribute into the
                                     existing manifest; keepcompression preserves the STORED nested
                                     BOOT-INF jars Spring Boot's loader requires. -->
                                <jar destfile="${project.build.directory}/${project.build.finalName}-executable.jar" update="true" keepcompression="true">
                                    <fileset dir="${project.build.directory}/launcher-agent-root" includes="org/**"/>
                                    <manifest>
                                        <attribute name="Launcher-Agent-Class" value="org.eclipse.dirigible.launcher.agent.DirigibleLauncherAgent"/>
                                    </manifest>
                                </jar>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>pl.project13.maven</groupId>
                <artifactId>git-commit-id-plugin</artifactId>
                <executions>
                    <execution>
                        <id>get-the-git-infos</id>
                        <goals>
                            <goal>revision</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <dotGitDirectory>../../.git</dotGitDirectory>
                </configuration>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>
</project>
